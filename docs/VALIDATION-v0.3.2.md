# Flomo Safe Sync v0.3.2 验证记录

验证日期：2026-09-05

## 自动化检查

`npm run check` 通过：

- TypeScript 类型检查通过。
- 基础同步核心测试通过，新增日期格式占位符、完整笔记模板渲染及结构拒绝测试。
- 受管区安全测试通过：38 个拒写场景、13 个兼容场景。
- 插件安全流程测试通过：18 项。
- v0.3.x 同步、迁移、删除和附件流程测试通过：60 项。
- 生产构建通过。

v4 设置迁移测试确认：旧 `yamlTemplate` 会进入完整笔记模板的用户 YAML 区；迁移不修改输入对象，当前版本设置可重复加载。

## Obsidian 隔离库

在独立测试库 `/private/tmp/flomo-safe-sync-qa-v030/vault` 使用 Obsidian 1.13.7 验证：

- 重启后插件清单显示 v0.3.2。
- 五个水平标签页可用，“笔记模板”替代旧“YAML 模板”。
- `{{yyyy-MM-dd}}_{{HHmmss}}_{{title:6}}` 实时生成 `2026-09-01_080910_第一条写作想.md`。
- 完整笔记源码可编辑；草稿切换标签页后保留，保存后从插件数据重新读取一致。
- 删除 `{{flomo_content}}` 后保存被拒绝，上次有效模板保持不变。
- 首次导入预览显示属性受管区、标签合并区和用户可编辑区。
- 1280 像素宽窗口左右排列；560 像素窄窗口上下排列，标签栏横向滚动且页面无横向溢出。
- 明暗主题均完成截图检查。

原生文件接口验证通过：受管正文更新保留手写内容；归档、重启后恢复、回收站执行成功；图床链接替换后本地文件已删除，后续同步保留远程链接且附件错误数为 0。

## 安装包

`npm run package` 通过。`flomo-safe-sync-0.3.2.zip` 仅含：

- `flomo-safe-sync/main.js`
- `flomo-safe-sync/manifest.json`
- `flomo-safe-sync/styles.css`

ZIP CRC 检查通过，不含 `data.json`。发布目录同时生成 BRAT 所需的三个独立运行文件及 `SHA256SUMS.txt`。

本地安装包 SHA-256：`460c9c990d8883778aa651321370850c61eb97fb8108f73d0eeb17bd21e2ba6f`。

## 验证边界

未使用用户真实 Flomo 账号执行同步；接口流程使用虚构响应和隔离库。用户真实 Vault 未安装本测试版。GitHub 上传后还需独立下载公开 Release 文件并复核校验值。
