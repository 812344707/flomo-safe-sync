# Flomo Safe Sync v0.3.5 验证记录

验证日期：2026-09-05

## 自动化检查

`npm run check` 通过：

- TypeScript 类型检查和生产构建通过。
- 同步核心测试通过。
- 受管区安全测试通过：38 个拒写场景、13 个兼容场景。
- 插件安全流程测试通过：18 项。
- v0.3.x 同步、迁移、删除和附件流程测试通过：62 项。

此次变更只复用现有设置字段和完整标签精确匹配逻辑，没有变更配置版本或同步数据结构。

## Obsidian 隔离库

在独立测试库 `/private/tmp/flomo-safe-sync-qa-v030/vault` 使用 Obsidian 1.13.7 验证：

- 标签树初始只显示顶层标签，展开父级后逐级显示子级。
- 点击 `#工作` 后，`#工作/项目` 与 `#工作/项目/甲` 同时选中并保存。
- 取消 `#工作/项目` 后，该标签及其子级同时取消，`#工作` 显示“部分选择”。
- 搜索 `甲` 时自动显示 `#工作 → #项目 → #甲` 的完整路径，展开状态使用有效的布尔无障碍属性。
- “仅看已选”只保留已选标签及其上级路径。
- 标签范围和专属目录在同一行设置；目录映射可新增、清除并在高级设置中调整优先级。
- 切换排除模式后，已排除标签的目录输入被禁用，原有目录映射和顺序继续保留。
- 560 像素窄窗口中标签与目录上下排列，无横向溢出；亮色和暗色主题显示正常。
- 水平设置标签页、模板草稿、保存、错误保留和重载持久化回归正常。

原生文件接口回归通过：正文更新、归档、重启后恢复、回收站和图床链接保留流程正常。

测试只使用虚构 memo 和隔离 Vault，没有连接用户真实 Flomo 账号，也没有改动用户真实 Vault。

## 安装包

`npm run package` 与 ZIP CRC 检查通过。`flomo-safe-sync-0.3.5.zip` 只包含：

- `flomo-safe-sync/main.js`
- `flomo-safe-sync/manifest.json`
- `flomo-safe-sync/styles.css`

安装包不含 `data.json`。GitHub Release 同时提供 BRAT 所需的三个独立运行文件、源码包、校验文件和本验证报告。

本地安装包 SHA-256：`8eef2e560b1e7a4d777889ccfa0a84e06c21222f796db3bfed0cef10ed7969b7`。

GitHub 上传后会从公开 Release 地址重新下载全部资产并复核摘要。
